<% if notice %>
  <p class="alert alert-success"><%= notice %></p>
<% end %>
<% if alert %>
  <p class="alert alert-danger"><%= alert %></p>
<% end %>

<% @cities.each do |city| %>
  <div id="index-banner" class="parallax-container">
    <div class="section no-pad-bot">
      <div class="container">
        <br><br>
        <h1 class="header center teal-text text-lighten-2"><%= city.name %></h1>
        <div class="row center">
          <h5 class="header col s12 light">Travel Stories</h5>
        </div>
        <div class="center">
          <button id="download-button" class="btn-large waves-effect waves-light teal lighten-1">View <%= link_to city.name, city%>'s Stories</button>
        </div>
        <br><br>
      </div>
    </div>
    <div class="parallax"><%= image_tag("#{city.photo_url}") %></div>
  </div>
<div>
<ol></ol>
</div>
<% end %>

<div id="index-banner" class="parallax-container">
    <div class="section no-pad-bot">
      <div class="container">
        <div class="row center">
          <h5 class="header col s12 light bottom-img">So many cities, so many extrodinary stories!</h5>
        </div>
      </div>
    </div>
    <div class="parallax"><%= image_tag("atlanta.jpg") %></div>
  </div>

